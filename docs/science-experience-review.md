# 互動教材視覺與自學體驗更新

範圍：兩批 20 頁共用版面與自學指引；顯微鏡、遺傳、力與運動、月相四頁的重點互動與圖像。

## 已實作

- 全部 20 頁：三步自學起點，包含本單元的第一個操作、觀察重點與比較任務；取消遮擋內容的浮動段落導覽，補足容器收縮與錨點留白。
- 遺傳：修正通用表格 `min-width:920px` 誤套到龐氏方格的根因；方格可點擊或鍵盤操作，逐格解釋親代配子、子代基因型與表現型。加入豌豆性狀插畫。
- 力與運動：小車外觀改用插畫；運動區與受力比較區分開。各水平力分列，垂直力獨立顯示，修正 SVG 箭頭依線寬放大造成遮擋的問題。
- 顯微鏡：試片操作台支援滑鼠、觸控 Pointer Events、方向按鈕與鍵盤方向鍵／Home。方向與既有光學反像模型一致；有範圍限制、拖曳取消處理、每幀一次繪製，以及依狀態變動的下一步提示。
- 月相：地面月相與太空位置分成兩張視圖；月面紋理使用插畫，亮暗邊界由模型計算。新增四個角度捷徑，進階軌道條件收合；完成預測後可自由拖動滑桿即時觀察。自由觀察不沿用舊預測，也不能保存過期的觀察結果。

## 程式驗收

- 既有 20 教材互動、第二批 21 組模型回歸通過。
- 新增拖曳前置條件、反向位移、邊界、取消、鍵盤、換標本引導重設測試。
- 新增遺傳格子選取、切換性狀、月相捷徑、即時觀察與紀錄失效測試。
- 73 個月相角度的繪圖亮面面積，與解析式 `(1−cosα)/2` 誤差小於 0.001。
- JSDOM 不代表真實排版驗收；正式站桌面目視結果見下節。實體觸控手機、投影與列印需另行實測，不以 Pointer Events 單元測試替代。

## 維護

共用體驗來源：`scripts/science/experience.css`、`experience.mjs`，由 `build_science.mjs` 內嵌至各頁。顯微鏡繪圖另執行 `build_microscope.mjs`。圖片在 `assets/science/`，提示詞見 `science-art-prompts.md`。

核心模型與操作不依賴外部框架；離線使用若要保留插畫，請連同 `assets/science/` 一起下載並保留相對路徑。

## 2026-09-18 正式站驗收

PR #1 經授權合併到 main（42925f1），正式站已載入新版本。本輪以 Chrome 桌面瀏覽器、1348 px 視窗實際點選、拖曳及截圖檢查；測試 URL 帶 noga=1。

| 頁面 | 已確認結果 |
| --- | --- |
| 顯微鏡 | 真實滑鼠拖移有更新試片座標與反像方向；4× 到 40× 物鏡對應 40× 到 400× 總倍率、4.5 到 0.45 mm 視野；逐步提示更新；Home 可回中央。 |
| 遺傳 | 豌豆插畫載入；Tt × Tt 四格完整顯示且未被右側控制區裁切；點 tt 顯示兩親代各提供 t 及矮莖說明。 |
| 力與運動 | 小車插畫載入；受力分列，箭頭與文字未重疊；2 kg、4 N、光滑面時摩擦 -1.96 N、合力 2.04 N。 |
| 月相 | 月面紋理載入；兩視角並列；完成預測後切換自由觀察與望 180°，亮面 100%；舊預測清空且觀察紀錄按鈕停用。 |

四頁 documentElement.scrollWidth 均等於 clientWidth（1348 px），沒有整頁橫向溢出。檢查期間未見網站程式錯誤；環境擴充套件回報的 metadata 錯誤另行辨識，不算網站錯誤。

### 實測發現並修復

1. 力與運動：已選預測但第一次直接按「前進 0.5 秒」時未初始化結果。改為依 verified 狀態初始化；新增未預測不揭露、第一次 0.5 秒與第二次 1 秒回歸測試。
2. 顯微鏡：實際瀏覽器會依 range step=10 對座標取整，與方向按鈕的 5 單位步進衝突。兩軸改用 step=1，保留按鈕與鍵盤的 5 單位移動；測試加入 stepMismatch 檢查。提示移除依賴左右版面的「右側」字樣。

上述修復已通過針對性程式回歸；修復部署後的正式站重測結果記錄於 PR #1 說明。

本輪未驗收實體手機觸控、投影或列印，不將桌面測試等同於這些環境的通過。
