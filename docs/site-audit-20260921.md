# 2026-09-21 官網稽核修正

## 基準

GitHub `main`：`a5620b73ab963c7695dfc92fecb90487c72122d0`（PR #19）。使用者提供的 `bhcs-web-main(3).zip` 與此版本內容一致；稽核報告所稱「已直接修正」的四項並未包含在附件中。本 PR 從這個已上線版本修改，不覆蓋先前研究站對話框及短螢幕修正。

## 完成項目

- 手機深色頁尾包含底部聯絡列的預留空間，消除白邊；主站及教育觀點頁尾補週二 13:30 起。
- 表單操作時隱藏底部列，包含按鈕聚焦，避免輸入失焦時浮出的聯絡列攔截複製按鈕。
- 預約另開 LINE，保留原頁；加入分頁草稿、內容預覽、複製與清除。草稿恢復期限為 2 小時，儲存／剪貼簿被封鎖有備援提示。明確表示必須在 LINE 按傳送才會收到。
- 手機首頁主區保留一個試聽入口；學部卡片加上「了解課程」；聯絡頁提供「索取本期班表與費用」入口。
- 選單支援 Escape 關閉並返回按鈕焦點。
- 資源篩選採品牌紅色、手機完整換行，保留搜尋和分類互動。
- 教育觀點三頁統一官網 logo、導覽、方格紙、深色頁尾與手機聯絡列；兩篇正文逐字保留，文章及清單配圖標示「情境示意圖」。新增建置步驟維持後續模板一致。
- 三個課程頁和五個舊工具延伸連結移除過期的「先預測」流程描述，修改建置來源後重建。
- 研究站目錄移除被隱藏的重複 h1；保留無 JavaScript 時的原始目錄。
- 補舊 GEPT 頁 description／canonical，保留 noindex；主站與文章共用 CSS／JS 加版本參數。

## 驗證與交付

- 本機 Chromium 全部 97 頁 × 390／1280 寬度，加正常／封鎖儲存兩組預約流程，共 196 項通過。檢查本地資源錯誤、壞圖、水平溢位、頁尾空間、選單、篩選、文章導覽與表單操作。
- 預約流程驗證原生必填檢查、草稿重載、特殊字元、另開 LINE 參數、內容更新、複製失敗備援、清除、過期／損壞資料恢復。不向 LINE、信箱或其他收件端發送測試訊息。
- `npm test --prefix scripts`、整合檢查及 researcher batch 1–5 通過；整合檢查涵蓋 97 HTML、1,152 本地引用、91 sitemap 項目。
- 三個建置腳本重跑無差異；CI 強制執行建置後 `git diff --exit-code`。
- 追加全站瀏覽器 CI，保留既有 126 構圖、8 顯微鏡互動及 40 研究站操作檢查；Actions 保存截圖和結果供複核。
- 本機測試不代表 GitHub CI 或正式站已驗證。PR 需等所有 CI 綠燈才合併；合併後確認 Pages 部署及正式網址畫面。最新 CI／部署結果記錄於 PR。

## 尚需實際資料或後續工作

- **直接收件後端**：尚未取得可用端點及收件設定，因此仍是 LINE 流程，沒有宣稱網站已收到預約。
- **真實評論、教室照片、學費方式與班級人數**：未提供可核實資料，未自行填造。現有配圖已標示示意；新增費用詢問入口。
- **Logo 檔案瘦身、舊列印出題器手機控制尺寸**：此次未修改圖片或列印版面，仍列為後續優化。
- 全站自動掃描封鎖外部服務以保持可重現。Google Fonts 的實際載入與重點頁面另做人工畫面驗收；地圖、LINE App 內的操作與後端收件不屬於這個自動掃描的通過範圍。
