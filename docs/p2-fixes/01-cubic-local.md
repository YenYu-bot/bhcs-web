# P2 修正 01：三次函數局部一次式

根因：`cubic/global` 的 `combined` 題型呼叫不存在的 `localAt`，造成 `ReferenceError`。補上精確有理數計算 `f(r)` 與 `x=r` 附近一次項係數 `3ar²+2br+c`；不修改取樣池、數字範圍、題型輪替或輸出模板。

驗證：`node scripts/math-regressions/cubic-local.mjs`，三難度各直接 200 次，共 600 次；117 題 `combined` 由答案多項式獨立代入，並用對稱差分扣除三次項核算局部斜率；無例外，所有非空候選 `verify()` 通過。

## 修正前後固定種子樣本

前四列是正常候選，題面與答案逐字相同；第五列重現本 PR 唯一行為差異：修正前沒有題目而是例外，修正後使用原本既有模板與範圍成功完成同一候選。

| 難度／種子 | 修正前題面 → 答案 | 修正後題面 → 答案 | 比對 |
|---|---|---|---|
| basic / 1 | f（x）＝x³＋3x²−x，寫出圖形的大域特徵。 → y＝x³ | f（x）＝x³＋3x²−x，寫出圖形的大域特徵。 → y＝x³ | 逐字相同 |
| basic / 2 | f（x）＝−2x³＋6x²−4x−4，寫出圖形的大域特徵。 → y＝−2x³ | f（x）＝−2x³＋6x²−4x−4，寫出圖形的大域特徵。 → y＝−2x³ | 逐字相同 |
| advanced / 1 | f（x）＝x³＋3x²−2x，寫出圖形的大域特徵。 → y＝x³ | f（x）＝x³＋3x²−2x，寫出圖形的大域特徵。 → y＝x³ | 逐字相同 |
| advanced / 2 | f（x）＝−2x³＋12x²−21x＋5，寫出圖形的大域特徵。 → y＝−2x³ | f（x）＝−2x³＋12x²−21x＋5，寫出圖形的大域特徵。 → y＝−2x³ | 逐字相同 |
| challenge / 1 | `ReferenceError: localAt is not defined`（未產題） | f 的大域特徵為 y＝x³，在 x＝0 附近近似 y＝−3x−1，且 f（−4）＝−5。求 f（x）。 → f（x）＝x³＋3x²−3x−1 | 原候選成功產出 |

既有 `null` 拒絕仍由 `safeQuestion()` 重試，不屬本根因。未修改 UI 或列印樣式。
