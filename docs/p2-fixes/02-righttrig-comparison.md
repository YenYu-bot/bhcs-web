# P2 修正 02：三角比比較題

根因：篩選交叉比較角度時，在 b 初始化前引用 b。改為使用 filter 的候選值 x，排除 a+x=90° 的等值情形；保留原 5°～85° 角度池、三種題型及難度。

`node scripts/math-regressions/righttrig-comparison.mjs`：三難度各 200 次，600/600 無空值、無例外、verify 全過；從實際題面讀取函數及角度另算大小，涵蓋 same/cross/self，交叉題無互餘等值。原始版本被同一測試以初始化錯誤拒絕。

依賴前一修正分支，只包含本根因的程式、回歸測試與紀錄。整體 P2 尚未通過，不合併部署。
