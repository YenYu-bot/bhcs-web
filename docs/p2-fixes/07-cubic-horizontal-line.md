# P2 修正 07：三次函數局部水平線正負號

在修正缺少 localAt 後，獨立核對實際題面又重現：f(x)=2x³−12x²+18x−6 在 x=1 附近應近似 y=2，原 lineThroughHTML 卻輸出 y=−2。根因是斜率為零時，正截距也加上負號。

改為首項正數省略符號，保留負截距與非零斜率的加減號。題型、數值範圍與答案多項式不變；只修正錯誤的局部一次式題面。

先以同一測試重現 -2≠2 再修正。`node scripts/math-regressions/cubic-horizontal-line.mjs` 直接 1,000 次挑戰題，所有 combined 題逐一從印出的局部式代入 r 及 r+1，核對函數值與斜率，並確認含正截距水平線。原 cubic-local 600 次測試也增加實際題面核對且通過。

整體 P2 尚未通過，不合併部署。
