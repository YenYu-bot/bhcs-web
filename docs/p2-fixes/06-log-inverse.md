# P2 修正 06：反函數驗證的浮點消去

原驗證以固定 x=.2,.7,1.3 代入。h 很大時 b^(x-h) 極小，加 k 再減 k 丟失精度，正確反函數被誤拒。改在 x=h+offset 取樣，保持 b^(x-h) 在穩定區間。原 H.near 容差不變，題目與答案逐字保留。

`node scripts/math-regressions/log-inverse.mjs`：三難度共 600/600 無空值、無例外、verify 全過。五種底數及全部 19 種 h 均覆蓋；檢查實際答案的符號平移式，另在反函數定義域中的穩定點做複合驗算。原版由同一回歸拒絕。

整體 P2 尚未通過，不合併部署。
