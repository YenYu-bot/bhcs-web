# G11 P4：`vectorcauchy` 的柯西最值

日期：2026-09-23。僅修改 `tools/math/g11-drills.html` 的 `cauchy` 單元和本主題難度說明。

## 題型與驗證

原本只有二元線性式最大值。現在基礎加二元最小值及絕對值最大值；進階加三元最大／最小值；挑戰加由最大／最小值反求正係數。係數取整數畢氏二元組或三元組，約束半徑平方取整數平方。

出題用係數向量長度乘以半徑；`verify()` 則讀回顯示答案，代回獨立建構的等號取到點，確認平方約束及線性式值。反求係數另把答案平方後代入係數長度恆等式，檢查 `b>0`。

## P3 棘輪（每難度 200 題）

| 單元 | 基礎 | 進階 | 挑戰 | 挑戰相對基礎新增 |
| --- | ---: | ---: | ---: | ---: |
| `vectorcauchy/cauchy` 原基準 | 1 | 1 | 1 | 0 |
| 本版 | 3 | 7 | 9 | 6 |

正規化仍區分正負號、係數位置、比較符號與 HTML 上下標，因此測得的結構數可能高於語意題型數。P3 變更單元 3（另兩個是此前已合併的 `sincosarea` 基準差），棘輪失敗 0。

## 每個新題型的三題樣本

| 新題型（難度） | 題目 | 答案 |
| --- | --- | --- |
| 二元最小值（basic）1 | 實數 x、y 滿足 x²＋y²＝81，求 7x＋24y 的最小值。 | −225 |
| 二元最小值（basic）2 | 實數 x、y 滿足 x²＋y²＝144，求 7x＋24y 的最小值。 | −300 |
| 二元最小值（basic）3 | 實數 x、y 滿足 x²＋y²＝49，求 5x＋12y 的最小值。 | −91 |
| 二元絕對值最大值（basic）1 | 實數 x、y 滿足 x²＋y²＝16，求 \|7x＋24y\| 的最大值。 | 100 |
| 二元絕對值最大值（basic）2 | 實數 x、y 滿足 x²＋y²＝100，求 \|20x＋21y\| 的最大值。 | 290 |
| 二元絕對值最大值（basic）3 | 實數 x、y 滿足 x²＋y²＝100，求 \|20x＋21y\| 的最大值。 | 290 |
| 三元最大值（advanced）1 | 實數 x、y、z 滿足 x²＋y²＋z²＝81，求 2x＋6y＋9z 的最大值。 | 99 |
| 三元最大值（advanced）2 | 實數 x、y、z 滿足 x²＋y²＋z²＝16，求 x＋2y＋2z 的最大值。 | 12 |
| 三元最大值（advanced）3 | 實數 x、y、z 滿足 x²＋y²＋z²＝100，求 2x＋6y＋9z 的最大值。 | 110 |
| 三元最小值（advanced）1 | 實數 x、y、z 滿足 x²＋y²＋z²＝100，求 2x＋6y＋9z 的最小值。 | −110 |
| 三元最小值（advanced）2 | 實數 x、y、z 滿足 x²＋y²＋z²＝144，求 x＋4y＋8z 的最小值。 | −108 |
| 三元最小值（advanced）3 | 實數 x、y、z 滿足 x²＋y²＋z²＝36，求 x＋2y＋2z 的最小值。 | −18 |
| 由最大值反求正係數（challenge）1 | 實數 x、y 滿足 x²＋y²＝16。已知 b＞0，7x＋by 的最大值為 100，求 b。 | b＝24 |
| 由最大值反求正係數（challenge）2 | 實數 x、y 滿足 x²＋y²＝100。已知 b＞0，20x＋by 的最大值為 290，求 b。 | b＝21 |
| 由最大值反求正係數（challenge）3 | 實數 x、y 滿足 x²＋y²＝144。已知 b＞0，3x＋by 的最大值為 60，求 b。 | b＝4 |
| 由最小值反求正係數（challenge）1 | 實數 x、y 滿足 x²＋y²＝100。已知 b＞0，20x＋by 的最小值為 −290，求 b。 | b＝21 |
| 由最小值反求正係數（challenge）2 | 實數 x、y 滿足 x²＋y²＝36。已知 b＞0，20x＋by 的最小值為 −174，求 b。 | b＝21 |
| 由最小值反求正係數（challenge）3 | 實數 x、y 滿足 x²＋y²＝1。已知 b＞0，3x＋by 的最小值為 −5，求 b。 | b＝4 |

## 自動測試

- 純 Node 檢查 `vectorcauchy`：每個單元 × 三難度 × 適用數型各 200 題，候選 `verify()` 失敗 0、例外 0、卷內重複 0；本單元結構觀察值 3／7／9（快速檢查器採另一套結構正規化，顯示 3／7／9）。
- 正式 `MATH_DIVERSITY_GATE=1 node scripts/test_math_drills.mjs`：全站 1740／1740 組通過，348000／348000 題產出，候選 verify 失敗 0、例外 0、棘輪失敗 0。

## 未驗證到的

- 三難度學用／教用瀏覽器截圖、列印視覺檢查尚待 PR 的 Chromium CI 產物；截圖與逐張人工檢視完成後補進本報告。
- 教材內容的最終審閱屬使用者的內容閘門。
