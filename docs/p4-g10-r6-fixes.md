# r6：依 ChatGPT 閘門報告（r5，2026-09-23）修正 11 項 P3 失敗

r6 ＝ r5 ＋ 以下。用 r6 整包覆蓋 #45。

## 為什麼 r5 會被擋

閘門的結構正規化把數字全部換成 #，所以「分子帶係數」「從中間項開始」「間隔為 2」這幾種我在 r4 加的變化，正規化後和原題完全相同（`計算 #/(#×#)＋⋯` ），不算新結構。我本機檢查器的正規化其實一樣，是我沒有逐單元看基礎級的數字。這一輪新增的結構全部是**題面文字不同**的形式，不靠數字差異。

| 閘門回報 | 新增（依難度） | 本機結構數（基礎／進階／挑戰） |
|---|---|---|
| `rationalexp/telescope` 1/1/1 | 基礎：拆項（將 1/(k(k+1)) 拆成兩分數之差）、Σ 記號；進階：已知和求 n；挑戰：Σ 1/((2k−1)(2k+1))、和大於某值的最小 n | 3／3／3 |
| `radicals/simplify` 基礎 2 | 基礎加「兩根式相乘」；挑戰加三項加減 | 3／3／3 |
| `radicals/rationalize` 2/2/3 | 基礎加分母為差；進階改為 兩項和／差／分子含根式 | 3／3／3 |
| `radicals/integerpart` 基礎 2 | 基礎加 c＋√n 的整數部分 | 3／3／3 |
| `radicals/telescope` 1/1/2 | 基礎：單項有理化、Σ 記號；進階：已知和求 n；挑戰：Σ 1/(√(2k+1)+√(2k−1)) | 3／3／3 |
| `logarithms/definition` 基礎 1 | 基礎加「指數式改寫成對數式」、真數為分數（題面加括號與「的值」以區別） | 3／3／3 |
| `logarithms/ratio` 基礎 2 | 基礎加 logA＋logB 求 AB、已知 B 求 A | 3／4／3 |
| `logarithms/digits` 2/2/3 | 基礎加「比較 bᵉ 與 cᶠ 大小」；進階改為位數／零的位置／首位數字 | 3／3／3 |
| `logarithms/growth` 基礎 2 | 基礎加反求倍率 | 3／5／5 |
| `quadratic/discriminant` 進階 25→24 | 進階加「與直線的交點個數」 | 本機 33 |
| `linecircle/minmax` 基礎 7→6 | 基礎加註記「P 在圓內」的形式 | 本機 9 |

## 未動的低結構單元（不在本次變更範圍，棘輪不會擋）

`radicals/nested`、`radicals/conjugate`、`logarithms/laws` 各難度結構數 2。它們是 r4 沒改的單元；若要一併拉到 3，請列入 P4 後續，我可以做。

## 本機驗證

七個 topic 逐數型、每 40 題一份卷去重、直接呼叫 `gen()` 統計原始例外：全部 PASS，原始例外 0、原始 verify 失敗 0；上表單元三難度結構數皆 ≥ 3。瀏覽器三難度學用／教用渲染無溢出。
