# 月相盈虧互動教室

純靜態網頁：`index.html` + `img/`（6 張圖）。整個資料夾原封不動上傳到網站任一路徑即可，路徑皆為相對路徑。

- 圖片缺檔時程式會自動退回向量繪圖，不會壞掉。
- 教學參數在 `index.html` 最上方的 `CFG`（軌道半徑、朔望月天數等）與 `PAGES`（各分頁的說明文字與思考題）。
- 所有程式、圖形、文字均為原創；圖片為 AI 生成的通用天文素材。
